import 'package:flutter/material.dart';

class Jobbutton extends StatelessWidget {
  const Jobbutton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
